overwrite: true
schema: "http://localhost:1337/graphql" # replace with your Strapi server URL
generates:
  packages/frontend/src/lib/constants/types/cms.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
